import React from 'react';

function Timer() {
    // Countdown timer for each question
    return (
        <div className='timer'>
            {/* Display countdown timer */}
        </div>
    );
}

export default Timer;
